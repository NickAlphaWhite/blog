---
// Admin panel: deployed separately
import { allLangs } from "../../lib/utils";
export function getStaticPaths() {
  return allLangs.map(l => ({ params: { lang: l } }));
}
const lang = Astro.params.lang || "zh";
return Astro.redirect(`https://admin.nickalphawhite.top/${lang}`);
---
