---
// Admin panel runs on port 3456 (separate server)
import { allRegions } from "../../lib/utils";
export function getStaticPaths() {
  return allRegions.map(r => ({ params: { lang: r } }));
}
return Astro.redirect("http://localhost:3456/admin?site=3000");
---
