---
// Admin panel: deployed separately
import { allRegions } from "../../lib/utils";
export function getStaticPaths() {
  return allRegions.map(r => ({ params: { lang: r } }));
}
const lang = Astro.params.lang || "";
return Astro.redirect(`https://admin.nickalphawhite.top/admin/${lang}`);
---
